const AddPropertyButton = () => {
    return (
       <div className="cursor-pointer p-2 text-sm font-semibold rounded-full hover:bg-gray-200">
            PWRoom is your one-step
       </div>
    );

};

export default AddPropertyButton;